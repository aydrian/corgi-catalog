[build]
command = "npm run build"
publish = "out"

[functions]
node_bundler = "esbuild"
external_node_modules = [
  "sequelize/lib/query-interface",
  "sequelize/lib/promise",
]
included_files = ["netlify/certs/*"]
